Разница между объявлениями переменных: 
let - Позволяет объявлять переменные, которые в любой момент могут измениться. 
const - С const в любой момент нельзя будет изменить, мы только указываем какое значение будет в этой переменной.

